# Save exact versions (no ^/~) when installing new packages, so package.json
# never drifts. Combined with the committed package-lock.json and `npm ci`,
# every install is fully reproducible.
save-exact=true
